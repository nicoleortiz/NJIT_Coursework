using System;

namespace MathOperations
{
    public static class Logarithms
    {
        static public double Logarithm(double antilog, double bbase)
        {
            return Math.Log(antilog, bbase);
        }
    }
}